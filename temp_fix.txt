            {activeTab === "chatbot" && (
              <div className="h-[calc(100vh-12rem)]">
                <ChatWidget currentUser={currentUser} />
              </div>
            )}
          </div>
        </div>
      </div>
    </>
  );
